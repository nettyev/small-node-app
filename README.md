# small-node-app

A small functioning Node.js web app with a dependency-free HTTP server.

## Requirements

- Node.js 18 or newer

## Run locally

```bash
node server.js
```

Then open http://localhost:3000 in your browser.

## API

- `GET /` returns a welcome message.
- `GET /health` returns the server health status.
